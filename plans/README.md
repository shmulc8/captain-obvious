# Implementation Plans

Generated by the improve skill on 2026-07-22, against commit `0eaabb5`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Baseline at plan time: `python3 -m unittest discover -v tests` → 62 tests,
`OK` (with `npm install typescript@^5 --no-save` done at the repo root —
without it, all TS-scanner tests silently skip).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fixer line integrity (control-byte desync + CRLF + partial-path tests) | P1 | M | — | DONE |
| 002 | Symlink write guard (fixer py/ts + mypy shadow) | P1 | S | 001 | DONE |
| 003 | Deletion-path test fixtures (TS type-guaranteed + guards, mock-echo/boundary/const-echo, coverage formats) | P1 | M | — | DONE |
| 004 | Hygiene batch (silent-smoke docs, isNoiseCall hoist, CLAUDE.md, CI skip guard) | P2 | S | — | DONE |
| 005 | Version floors (py ≥3.9, ts ≥4) + CI hardening (permissions, pinned checkout/typescript) | P2 | S | 004* | DONE |
| 006 | TS laundering transitivity (verify, then fix) | P2 | M | — | DONE — CONFIRMED: indirect launder was `type-guaranteed/proven/safe`, direct unflagged; fixed w/ bounded transitive recursion |
| 009 | unittest assert-methods (report-only detection) | P2 | M | 001 | DONE — reconciled single loop (probes=[], QUEUED→nonredundant); findings forced report-only, --fix inert |
| 007 | Determinism + parity (sorted TS discovery, snippet docs, parity corpus) | P3 | S–M | — | DONE — 12-subtest corpus green; dead-assert fixture isolated w/ a live assert (dead-only collapses to never-asserts on both engines — not a real break) |
| 008 | Real-mypy opt-in end-to-end test | P3 | S | 005* | DONE — 4/4 pass against real mypy (installed via uv); reveal_type contract holds |
| 010 | Plain-JS test files (syntactic categories, type-gate) | P3 | M | 007 | DONE — JS discovery widened; fileTypes=typesAvailable&&!isJs gate keeps type-guaranteed off JS |
| 011 | `--check --base` CI/pre-commit gate for new proven findings | P3 | M–L | — | DONE — built (report-only, new-proven-syntactic only, fail-open); exit-code contract verified on both CLIs; action.yml + .pre-commit-hooks.yaml added |
| 012 | SPIKE: Go back-end feasibility | P3 | L | — | BLOCKED — no Go toolchain in this environment (`which go`/`go version`/`/usr/local/go` all absent); spike needs go1.2x+; no spike/co_go created, skills/ untouched |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned).

## Dependency notes

- **002 after 001**: both rewrite `co_py/fixer.py`'s write path; 001
  restructures it, 002 adds the guard on the restructured code.
- **009 after 001**: 009 relies on the fixer invariants 001's tests pin
  (report-only findings must be provably inert under `--fix`).
- **010 after 007**: both edit `co_ts/discovery.mjs` (007 adds `.sort()`,
  010 widens the regexes).
- **004 → 005 → 008 (\*ordering, not hard deps)**: all three edit
  `.github/workflows/tests.yml`; execute in index order to avoid conflicts.
  Their code changes are otherwise independent.
- 003, 006, 007, 011, 012 are independent and parallel-safe (003/006 both
  create TS fixture tests but in different files).
- 006 and 011 begin with a verify/decision gate — their status may
  legitimately end REJECTED; that is a valid outcome, record the evidence.

## Findings considered and rejected

(So nobody re-audits them.)

- **Lint/format/typecheck config for the scanner's own code**: the
  stdlib-only / zero-dep constraint is deliberate (skill must run bare when
  copied into `~/.claude/skills/`); a dev-only config is possible but the
  payoff on ~4.6k lines ranks below everything planned. Revisit only if
  contributor count grows.
- **Coverage-subsumption detector**: lcov/istanbul/coverage.py emit
  aggregate suite coverage, not per-test line attribution; subsumption needs
  per-test coverage runs the runners don't produce by default. Correctly
  out of scope (also stated in README limitations).
- **Unifying the py/ts engines**: L effort against the portability
  constraint; plan 007's parity corpus + CLAUDE.md parity rule are the
  chosen mitigation instead.
- **Adding `snippet` to Python findings**: documented as a TS-only optional
  field instead (plan 007) — cheaper, zero risk, no consumer asked for py
  parity.
- **mock_echo `toHaveBeenCalledTimes(1)` looseness**: audited and fine —
  only flagged when the test itself makes exactly one direct call; two calls
  already fail the test.
- **Hook `--stdin` re-reading disk in `analyzer.py:488`**: harmless —
  single-file mode discards probes; verified during audit vetting.
- **gitguard timeout/error paths**: fail toward BLOCKING `--fix` — correct
  direction, no change needed.

## Source audit

Full audit (4 parallel auditors: correctness, security+deps,
tests+debt+DX+docs, direction) vetted against the code at `0eaabb5` on
2026-07-22. Two fixer bugs (plan 001) were reproduced end-to-end before
planning. Not examined: `assets/`, `.remember/`, caches; detector
false-positive rates on external repos (only this repo's own suite).
