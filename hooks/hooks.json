{
  "description": "Write-time prevention: blocks Write/Edit calls that introduce proven can-never-fail test patterns. Off by default. Set CAPTAIN_OBVIOUS_HOOK=block or =warn to enable.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/prevent.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
